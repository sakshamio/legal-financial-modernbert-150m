# Stage 1 stack (verified on torch 2.9.0+cu130 / aarch64 / GB10 sm_121)
torch>=2.9
transformers>=4.56
tokenizers>=0.22
datasets>=4.1
accelerate>=1.10
huggingface_hub>=0.35
numpy>=2.1
pyarrow>=17

# Stage 2 (contrastive + Matryoshka)
sentence-transformers>=5.6
rank_bm25>=0.2          # hard-negative mining

# Go 1.23+ required for godedup/ (see README)
